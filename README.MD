#Shopping-Cart-Contenarized

This is a version of a shopping cart with both backend and frontend fully working inside docker containers.

In my original version I use the fakestoreapi here I created a mock backend with only the GET endpoints to mock the same funtionality